var dice = new Array(5).fill(0),
    diceFixed = new Array(5).fill(false),
    throwNumber = 0;
var diceElement = document.getElementsByClassName("dice");
var rollButtonElement = document.getElementById("rollButton");
// Resets the dice to new roll of dice (after scoring a category)
var diceReset = function () {}
// Roll the dice. Check for user holding of dice
var diceRoll = function () {}
// User toggles dice for holding of dice
var diceToggle = function(index){}